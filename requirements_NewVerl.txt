# SeeUPO 环境安装指南（非配置文件）
# 环境配置来源: seeupo_env.yaml（导出的配置）
#
# ========== 安装步骤 ==========
#
# 1. 创建 conda 环境
#    conda create -n seeupo python=3.10
#    conda activate seeupo
#
# 2. 按 seeupo_env.yaml 安装包（不含 verl / flash-attn / vllm） （如果一次安装不完，后面报错的时候参考这个yaml就行）
#    conda env update -f seeupo_env.yaml -n seeupo
#
# 3. 按顺序单独安装（指定版本）:
#    pip install -e external/verl --no-deps
#    pip install flash-attn==2.7.0.post2 --no-deps --no-build-isolation
#    pip install vllm==0.8.5

# 4. 如果遇到任何异常或冲突，请直接严格按照seeupo_env.yaml中的包的版本来配置
# =====================================================
